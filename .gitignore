# Rust/Cargo build output (workspace at rust/, plus the standalone prexp-desktop
# crate and any Swift SwiftPM builds) — match `target` anywhere, not just root.
target/

# Swift / Xcode
.build/
.swiftpm/
*.xcodeproj
DerivedData/
